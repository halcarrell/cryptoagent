# Secrets — NEVER commit these.
.env
.env.*
*.env
secrets.json
**/credentials.*

# Local DB — contains historical alert payloads + market data; never push
# to a repo. Volume-backed copies live on Railway and stay there.
*.db
*.db-journal
*.db-wal
*.db-shm
crypto_agent.db*

# Generated artifacts from tv_integration.py
watchlist_*.txt
scores_*.pine

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/

# Logs
*.log
agent.log

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# weights.json is committed as a baseline; the weekly refitter overwrites
# /data/weights.json on the Railway volume, which takes precedence at runtime.
